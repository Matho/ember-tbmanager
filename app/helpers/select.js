/*import Ember from 'ember';

export function select(variable,value) {
  if (variable == value) {
    return new 'selected=selected';
  }
  else {
    return '';
}

export default Ember.Helper.helper(select);



*/

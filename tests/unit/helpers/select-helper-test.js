
import { selectHelper } from 'tbmanager/helpers/select-helper';
import { module, test } from 'qunit';

module('Unit | Helper | select helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectHelper([42]);
  assert.ok(result);
});

